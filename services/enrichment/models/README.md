# Pretrained YOLO ONNX weights (not committed).

```bash
python services/enrichment/scripts/download_cv_models.py
```

Expected files:

- `plate_yolov8n.onnx` — license plates
- `scene_text_yolo11n.onnx` — scene text (watermark path)
